# Security Practices

- Legal-access-only policy (no paywall bypass)
- OAuth with short-lived access tokens and refresh rotation
- Input validation and ORM query safety
- Rate limits for AI endpoints
- Secret management via environment variables
- Audit logging for auth and destructive actions
