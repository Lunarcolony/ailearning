# Frontend Architecture

## Routes
- `/feed`, `/search`, `/paper/[id]`, `/journey/[id]`, `/library`, `/collections/[id]`, `/settings`

## Principles
- Dark-mode-first, mobile-friendly, exploration-centric UI
- Fast card interactions with recommendation reasons
- API-driven server state, optimistic user feedback actions
